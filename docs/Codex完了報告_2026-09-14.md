# Codexへの引き渡し：完了報告

報告日：2026-09-14。問い合わせフォームの実装・公開と、追加依頼による入口の一本化・電話番号必須化まで完了。レビュー待ち。

## 成果物

- 公開フォーム：https://ccjcapoeira.github.io/ccj-inquiry-form/
- ソース：https://github.com/ccjcapoeira/ccj-inquiry-form
- レビュー対象コミット：`19b2821`（main、公開反映済み）
- ローカル：`/Users/kuboyamatakeshi/GitHub/ccj-inquiry-form`
- 主なファイル：`index.html`、`google-apps-script.js`、`redirects.json`、`scripts/build-go.js`、`go/`、`docs/`、`verification/`

## 実装・公開した内容

1. 入力→確認→完了の問い合わせフォーム。キッズ年齢、認知経路の条件付き項目、UTM・参照元保存に対応。
2. 電話番号はメールアドレス直後に常時表示し、必須入力。GASでも空欄・空白のみ・欠落を拒否する。
3. CCJ所有の非公開スプレッドシートに指定32列で保存。受付ID採番、入力検証、数式混入対策、自動返信・管理通知を実装。メール失敗時も台帳行を保持。
4. GASを公開し、GitHub Pagesの本番フォームへ接続。GASはレビュー対応後のバージョン5まで反映済み。
5. 12件のQR用転送ページと一覧を生成。QR画像そのものの印刷・配布は未実施。
6. Goopeのサイト内CONTACT・教室案内リンクを新フォームへ直結。旧 `/contact` は入力欄を撤去して新フォームへ自動転送。過去のGoope問い合わせ履歴・受信設定は維持。
7. セットアップ、UTM運用、台帳運用、Looker Studio構築の手順書を作成。本人向け手順はローカルの `.local/本人が行う操作.md`。

## 検証結果

- 初回ブラウザ検証：必須項目、条件表示、確認から戻る操作、UTM保持、送信中ロック、通信失敗、ストレージ不可、12転送ページ、320〜1280pxの表示を確認。
- 本番実送信：2026-09-13、受付ID `INQ-20260913-001`。台帳1行、UTM、日本時間、自動返信・管理通知2通の受信を確認。検証行に「集計対象外」のメモを記入済み。
- Goope：教室案内内とナビゲーションの問い合わせリンク12件のURLを確認。クリックによる新フォームへの直接移動と、旧URLからのUTM付き転送を確認。
- 電話番号必須化後：GAS構文・模擬テスト合格。本番GAS更新、Pagesデプロイ成功、公開画面で常時表示と「必須」表示を確認。必須化後の実送信も実施済み（下記追記）。ブラウザ回帰スイート全体の再実行は未実施。

## レビュー時の留意点・未実施事項

- 指示書の指定32列と「全ペイロードキーを1対1保存」は両立しないため、32列を優先。`privacyAgree` は検証のみ、`userAgent` 原文は保存しない。外側 `formType` は振り分け用。対応表はREADME参照。
- 指定の `no-cors` 送信ではGASの応答をブラウザで読めない。完了画面だけでは保存成功を断定できない。
- UTMは新フォーム内で保持。旧contact URLに付いたUTMは転送時に引き継ぐが、公式サイトへの初回訪問UTMをサイト内で保存・継承する機能は未実装。
- Goopeの動的なナビゲーション・本文リンクは共通JavaScriptで書き換える。JavaScript無効時は旧contactページのmeta refreshへフォールバックし、UTMは引き継がない。
- Goopeの変更は管理画面の適用中デザイン「CCJ 週間カレンダー 2026-09」（ID 946573）へ直接反映。新フォームのGitコミットだけではGoope側の変更は再現されない。実装箇所・復元方法はセットアップ手順とローカルバックアップ参照。
- スマートフォン実機の入力確認、Looker Studioの実接続・レポート作成は未実施。Looker Studioは手順書まで。
- 広告キャンペーン `ad2026-10` とイベント `sample-event` は仮設定。配布前に確定が必要。
- 本番URLを設定済みのHTMLで送信すると実際に受付・メール送信が発生する。回帰スクリプトはデモHTMLと模擬送信を使用する。

## 次の担当者への依頼

上記成果物をレビューし、32列の例外とno-corsの制約を確認してください。必要に応じて実機確認、Looker Studio構築、媒体別URLの確定を続けてください。台帳・GASの再作成は不要です。実申込者データや認証情報は公開リポジトリに追加しないでください。

## レビューAへの対応完了（2026-09-14）

- doPostの例外時、管理者へ「【問い合わせ処理エラー】」を通知。取得できた氏名・メール・電話・希望教室を本文に含める。通知失敗時はログのみで再帰しない。
- ヘッダー不一致では保存を止めず、先頭32列へ位置固定で保存し、受付ID付きの警告を通知。追加列33列目以降は照合対象から外して保持。
- 警告通知は保存と通常メール処理の後、ロック解除後に実施。通知失敗で保存を取り消さない。
- GASバージョン5へ更新。電話番号入りの実送信を1回実施し、`INQ-20260914-002`（2026/09/14 18:11:17）の1行保存と通常メール2通を確認。検証行に集計除外メモを記入。
- GAS模擬テストで、連絡先入りエラー通知、ヘッダー変更後の保存と警告、追加列保持、メール片側失敗時の保存維持、エラー通知失敗時の非再帰を確認。
- 本番台帳のヘッダーを故意に変える障害試験は行っていない。異常系は模擬検証。
- 台帳運用に「1行目のヘッダーは変更しない。列の追加は33列目以降」を明記。

残る制約：Googleの送信上限・メール権限障害時はエラー通知自体も送れずログのみ。列の並べ替えを自動修復するものではない。レビューBの広告・GA4・SNS・GBP・サイト内UTM保持は今回未変更。
