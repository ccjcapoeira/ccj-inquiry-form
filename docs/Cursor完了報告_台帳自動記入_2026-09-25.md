# Cursor 完了報告 — 台帳の自動記入

日付: 2026-09-25 ／ 対象: リード台帳の体験日・入会日・見送りの自動記入

## 1. 実行した手順と結果

| 手順 | 結果 |
|------|------|
| 2-1 コード反映 | `node scripts/deploy-gas.cjs` 成功。`.gas/appsscript.json` に `gmail.readonly` と `script.scriptapp` あり。最終デプロイは @8 |
| 2-2 入会シートID | Script Properties に保存済み（ログ「入会申込シートIDを保存しました。」）。値はここに書かない |
| 2-3 更新APIの合言葉 | 発行済み。`~/Library/Application Support/CCJInquiryLedger/.env` に保存し `chmod 600`。値はここに書かない |
| 2-4 DryRun | 2回目が正。体験・入会 0件、見送り 0件。自動更新ログは `DRY / 変更なし` |
| 2-5 本番1回 | `dailyLedgerSync` 完了。記入 0件、見送り判定 0件。自動更新ログは `実行 / 変更なし` が2行 |
| 2-6 トリガー | 実行ログ「毎朝7時台の自動記入トリガーを登録しました。」新規登録（「登録済み」ではない） |
| 2-7 更新API | `scripts/ledger_update.py --id INQ-20260913-001 --memo "API疎通確認" --by cursor` が `result: success` |

clasp 標準のログイン画面は、Gmail 読み取りを求めると Google にブロックされる。権限承認は台帳に紐づく Apps Script の「実行」で行った。

一時関数（`_onceSetup` / `_onceDryRun` / `_onceApply`）は本番コードから外し、@8 で再デプロイした。

## 2. DryRun の件数

採用したのは2回目（2026-09-25 13:57）。

- 体験日・入会日: 対象 0件。目視でも 0件が正しい
- 見送り: 対象 0件。目視でも 0件が正しい

台帳の実データは6行。うち2行はメモに「集計対象外」があり対象外。残り4行は受付が 2026-09-14 以降で、30日ルールに届いていない。体験日・入会日は空欄のままで、入会申込シートのメールアドレスとも一致しなかった。

## 3. 対象外・判定できなかった行

| ID | 理由 |
|----|------|
| INQ-20260913-001 | メモが「集計対象外」。体験・入会・見送りの対象外。API疎通のメモ追記のみ |
| INQ-20260914-002 | メモが「集計対象外」 |
| INQ-20260914-001 | 受付から30日未満。お礼メールも入会申込の一致もなし |
| INQ-20260920-001 | 同上 |
| INQ-20260923-001 | 同上 |
| INQ-20260924-001 | 同上 |

## 4. 仮置き・判断に迷った点

1回目の実行ログは「体験・入会 8件」「見送り 31件」だった。同じ時刻の自動更新ログは「変更なし」だけで、台帳の体験日・入会日・ステータスも空欄のままだった。見送り判定はステータスと理由を組で数えるので 31 は奇数になり、コードの件数とも合わない。この件数は採用せず、ログの書き込みを `appendRow` に直してから DryRun をやり直した。2回目と本番はどちらも 0件で、台帳の中身と一致した。

直したのは `writeAutoLog_` のみ。`findTrialThankYou_` など判定ロジックは変えていない。この修正はリポジトリに未コミット。

既存セルの上書きはない。変わったのは指示どおり、`INQ-20260913-001` のメモへ「2026/09/25 API疎通確認」を追記したことだけ。

## 完了条件

- [x] `.gas/appsscript.json` に `gmail.readonly` と `script.scriptapp`
- [x] Script Properties に `ENROLLMENT_SPREADSHEET_ID` と `LEDGER_UPDATE_TOKEN`（値は非掲載）
- [x] `~/Library/Application Support/CCJInquiryLedger/.env`（Git 管理外）
- [x] 自動更新ログに DRY 行と実行行
- [x] トリガー `dailyLedgerSync` を新規登録（実行ログで確認。エディタのトリガー一覧は未表示）
- [x] `scripts/ledger_update.py` が success
- [x] 台帳の既存値は変わっていない（差分は指示どおりのメモ追記のみ）
